# 日志管理模块开发指南

> 本文档面向初学者，每一步都附有详细说明和代码示例，按照顺序即可逐步完成日志管理模块的开发。

---

## 目录

1. [模块概述](#1-模块概述)
2. [数据库设计](#2-数据库设计)
3. [后端开发](#3-后端开发)
4. [前端开发](#4-前端开发)
5. [菜单注册](#5-菜单注册)
6. [测试验证](#6-测试验证)

---

## 1 模块概述

### 1.1 目标

实现一个完整的日志管理模块，包含两大功能：

- **操作日志自动记录**：通过自定义注解 + AOP 切面，自动记录用户的操作行为（谁、在什么时间、做了什么操作、参数是什么、耗时多久、IP 地址等）。
- **日志管理页面**：前端提供日志列表查询页面，支持按操作人、操作类型、时间范围等条件筛选，支持批量删除。

### 1.2 技术要点

| 技术 | 用途 |
|------|------|
| `自定义注解` | 标注需要记录日志的 Controller 方法 |
| `Spring AOP` | 拦截带 `@Log` 注解的方法，自动写日志 |
| `MyBatis-Plus` | 日志表 CRUD 操作 |
| `HttpServletRequest` | 获取操作人 IP 地址等信息 |
| `Vue 3 + Element Plus` | 前端日志管理页面 |

### 1.3 涉及的文件清单

**后端（共 8 个文件）：**

| 文件 | 路径 |
|------|------|
| Entity | `mall-sys/src/main/java/com/mall/mallsys/modules/system/entity/SysLog.java` |
| Mapper | `mall-sys/src/main/java/com/mall/mallsys/modules/system/mapper/SysLogMapper.java` |
| Service | `mall-sys/src/main/java/com/mall/mallsys/modules/system/service/SysLogService.java` |
| ServiceImpl | `mall-sys/src/main/java/com/mall/mallsys/modules/system/service/impl/SysLogServiceImpl.java` |
| Controller | `mall-sys/src/main/java/com/mall/mallsys/modules/system/controller/SysLogController.java` |
| 自定义注解 | `mall-sys/src/main/java/com/mall/mallsys/common/annotation/Log.java` |
| AOP 切面 | `mall-sys/src/main/java/com/mall/mallsys/common/aspect/LogAspect.java` |
| DTO | `mall-sys/src/main/java/com/mall/mallsys/modules/system/dto/LogQueryDTO.java` |

**前端（共 3 个文件）：**

| 文件 | 路径 |
|------|------|
| API | `mall-sys-web/src/api/log.ts` |
| 页面 | `mall-sys-web/src/views/system/log/LogList.vue` |
| 路由 | `mall-sys-web/src/router/index.ts`（修改） |

---

## 2 数据库设计

### 2.1 建表 SQL

在数据库 `mall_sys` 中创建操作日志表，执行以下 SQL：

```sql
-- 操作日志表
DROP TABLE IF EXISTS `sys_log`;
CREATE TABLE `sys_log` (
    `id`            bigint       NOT NULL AUTO_INCREMENT  COMMENT '主键 ID',
    `username`      varchar(50)  DEFAULT NULL             COMMENT '操作人用户名',
    `operation`     varchar(100) DEFAULT NULL             COMMENT '操作描述',
    `method`        varchar(255) DEFAULT NULL             COMMENT '请求方法（类全限定名.方法名）',
    `params`        text         DEFAULT NULL             COMMENT '请求参数（JSON 格式）',
    `time`          bigint       DEFAULT NULL             COMMENT '执行时长（毫秒）',
    `ip`            varchar(64)  DEFAULT NULL             COMMENT '操作人 IP 地址',
    `browser`       varchar(100) DEFAULT NULL             COMMENT '浏览器信息',
    `status`        tinyint      DEFAULT '1'              COMMENT '操作状态：0-失败，1-成功',
    `error_msg`     text         DEFAULT NULL             COMMENT '错误信息',
    `create_time`   datetime     DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间（操作时间）',
    PRIMARY KEY (`id`),
    KEY `idx_username` (`username`),
    KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';
```

### 2.2 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 自增主键 |
| `username` | varchar(50) | 操作人的用户名，如 `admin` |
| `operation` | varchar(100) | 操作描述，来自 `@Log` 注解的 value 属性，如 "新增用户" |
| `method` | varchar(255) | 被调用方法的类全限定名 + 方法名，如 `com.mall...SysUserController.add` |
| `params` | text | 请求参数的 JSON 字符串 |
| `time` | bigint | 方法执行耗时，单位毫秒 |
| `ip` | varchar(64) | 客户端 IP 地址 |
| `browser` | varchar(100) | 浏览器 User-Agent 信息 |
| `status` | tinyint | `1`= 成功，`0` = 异常 |
| `error_msg` | text | 如果操作失败，记录异常堆栈信息前 500 字符 |
| `create_time` | datetime | 操作发生时间 |

### 2.3 为什么不用 `deleted` 字段？

日志表一般不需要逻辑删除，因为日志是审计数据，应该保留。且日志使用物理删除（批量清理历史数据），所以本表不设 `deleted` 和 `update_time` 字段。

---

## 3 后端开发

> 以下 8 个文件按顺序创建，每步都有完整的代码。

### 3.1 实体类 — SysLog.java

**路径：** `mall-sys/src/main/java/com/mall/mallsys/modules/system/entity/SysLog.java`

```java
package com.mall.mallsys.modules.system.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.Accessors;

import java.io.Serial;
import java.io.Serializable;
import java.time.LocalDateTime;

/**
 * 操作日志实体类
 */
@Data
@EqualsAndHashCode(callSuper = false)
@Accessors(chain = true)
@TableName("sys_log")
public class SysLog implements Serializable {

    @Serial
    private static final long serialVersionUID = 1L;

    @TableId(value = "id", type = IdType.AUTO)
    private Long id;

    private String username;

    private String operation;

    private String method;

    private String params;

    private Long time;

    private String ip;

    private String browser;

    private Integer status;

    private String errorMsg;

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;
}
```

**要点说明：**
- `@TableName("sys_log")` — 指定对应数据库表名。
- `@TableId(type = IdType.AUTO)` — 主键自增。
- 没有 `@TableLogic` — 日志不需要逻辑删除。
- `createTime` 使用 `FieldFill.INSERT` 自动填充（由 MyBatis-Plus 的 MetaObjectHandler 处理，如果你的项目中还没配置，可以改用数据库 `DEFAULT CURRENT_TIMESTAMP`）。

---

### 3.2 自定义注解 — Log.java

**路径：** `mall-sys/src/main/java/com/mall/mallsys/common/annotation/Log.java`

这个注解用来标记哪些 Controller 方法需要记录操作日志。

```java
package com.mall.mallsys.common.annotation;

import java.lang.annotation.*;

/**
 * 操作日志注解
 * 标注在 Controller 方法上，自动记录操作日志
 */
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface Log {

    /**
     * 操作模块 / 描述
     * 例："新增用户"、"删除角色"
     */
    String value() default "";
}
```

**要点说明：**
- `@Target(ElementType.METHOD)` — 只能标注在方法上。
- `@Retention(RetentionPolicy.RUNTIME)` — 运行时可通过反射获取注解信息（AOP 需要）。
- `value` 属性 — 用于描述这个操作是什么，如 "新增用户"。

**使用示例（标注在 Controller 方法上）：**

```java
@PostMapping
@Operation(summary = "新增用户")
@Log("新增用户")           // <-- 加上这个注解，调用此接口时会自动记录日志
public Result<Void> add(@RequestBody SysUser user) {
    ...
}
```

---

### 3.3 AOP 切面 — LogAspect.java

**路径：** `mall-sys/src/main/java/com/mall/mallsys/common/aspect/LogAspect.java`

这是整个模块最核心的部分。AOP 切面会拦截所有被 `@Log` 标注的方法，在方法执行前后自动记录日志。

```java
package com.mall.mallsys.common.aspect;

import com.alibaba.fastjson2.JSON;
import com.mall.mallsys.common.annotation.Log;
import com.mall.mallsys.modules.system.entity.SysLog;
import com.mall.mallsys.modules.system.entity.SysUser;
import com.mall.mallsys.modules.system.service.SysLogService;
import jakarta.servlet.http.HttpServletRequest;
import lombok.extern.slf4j.Slf4j;
import org.aspectj.lang.ProceedingJoinPoint;
import org.aspectj.lang.annotation.Around;
import org.aspectj.lang.annotation.Aspect;
import org.aspectj.lang.annotation.Pointcut;
import org.aspectj.lang.reflect.MethodSignature;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.stereotype.Component;
import org.springframework.web.context.request.RequestContextHolder;
import org.springframework.web.context.request.ServletRequestAttributes;

import java.lang.reflect.Method;
import java.time.LocalDateTime;

/**
 * 操作日志切面
 * 拦截 @Log 注解，自动记录操作日志到数据库
 */
@Slf4j
@Aspect
@Component
public class LogAspect {

    @Autowired
    private SysLogService sysLogService;

    /**
     * 定义切点：所有标注了 @Log 注解的方法
     */
    @Pointcut("@annotation(com.mall.mallsys.common.annotation.Log)")
    public void logPointCut() {
    }

    /**
     * 环绕通知：在目标方法执行前后记录日志
     */
    @Around("logPointCut()")
    public Object around(ProceedingJoinPoint joinPoint) throws Throwable {
        // 1. 记录开始时间
        long startTime = System.currentTimeMillis();

        // 2. 获取 HttpServletRequest 对象
        ServletRequestAttributes attributes =
                (ServletRequestAttributes) RequestContextHolder.getRequestAttributes();
        HttpServletRequest request = attributes != null ? attributes.getRequest() : null;

        // 3. 获取执行方法和注解信息
        MethodSignature signature = (MethodSignature) joinPoint.getSignature();
        Method method = signature.getMethod();
        Log logAnnotation = method.getAnnotation(Log.class);
        String operation = logAnnotation != null ? logAnnotation.value() : "";

        // 4. 获取操作人
        String username = null;
        Object securityUser = SecurityContextHolder.getContext()
                .getAuthentication().getPrincipal();
        if (securityUser instanceof SysUser sysUser) {
            username = sysUser.getUsername();
        }

        // 5. 构建日志对象
        SysLog sysLog = new SysLog();
        sysLog.setUsername(username);
        sysLog.setOperation(operation);
        // 方法全限定名
        sysLog.setMethod(joinPoint.getTarget().getClass().getName() + "." + method.getName());

        // 请求参数（转 JSON，避免过长的参数）
        String params = JSON.toJSONString(joinPoint.getArgs());
        // 参数超过 5000 字符截断，防止数据库字段溢出
        if (params.length() > 5000) {
            params = params.substring(0, 5000);
        }
        sysLog.setParams(params);

        // 获取 IP 地址
        if (request != null) {
            sysLog.setIp(getClientIp(request));
            sysLog.setBrowser(request.getHeader("User-Agent"));
        }

        // 6. 执行目标方法，记录结果
        Object result;
        try {
            result = joinPoint.proceed();  // 执行原方法
            sysLog.setStatus(1);           // 标记成功
            return result;
        } catch (Throwable e) {
            sysLog.setStatus(0);           // 标记失败
            // 截取异常信息前 500 字符存入数据库
            String errorMsg = e.getMessage();
            if (errorMsg != null && errorMsg.length() > 500) {
                errorMsg = errorMsg.substring(0, 500);
            }
            sysLog.setErrorMsg(errorMsg);
            throw e;
        } finally {
            // 7. 计算执行耗时（无论成功失败都会执行）
            sysLog.setTime(System.currentTimeMillis() - startTime);
            sysLog.setCreateTime(LocalDateTime.now());
            // 8. 保存日志到数据库
            sysLogService.save(sysLog);
        }
    }

    /**
     * 获取客户端真实 IP 地址
     * 考虑了代理服务器（Nginx 等）转发的情况
     */
    private String getClientIp(HttpServletRequest request) {
        String ip = request.getHeader("X-Forwarded-For");
        if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
            ip = request.getHeader("X-Real-IP");
        }
        if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
            ip = request.getHeader("Proxy-Client-IP");
        }
        if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
            ip = request.getHeader("WL-Proxy-Client-IP");
        }
        if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
            ip = request.getRemoteAddr();
        }
        // 多级代理时取第一个 IP
        if (ip != null && ip.contains(",")) {
            ip = ip.split(",")[0].trim();
        }
        return ip;
    }
}
```

**要点说明：**

| 关键点 | 说明 |
|--------|------|
| `@Aspect` | 声明这是一个 AOP 切面类 |
| `@Pointcut("@annotation(...)")` | 切点表达式，匹配所有标注了 `@Log` 注解的方法 |
| `@Around` | 环绕通知，可以在目标方法执行前后都插入逻辑 |
| `joinPoint.proceed()` | 执行原方法，返回值就是原方法的返回值 |
| `finally` 块 | 无论方法成功还是异常，都会执行日志保存 |
| `params` 截断 | 防止参数过大（如文件上传）导致数据库字段溢出 |

> **⚠ 注意：这里需要 `fastjson2` 依赖。** 如果你的项目中还没有，请在 `pom.xml` 中添加：
>
> ```xml
> <dependency>
>     <groupId>com.alibaba.fastjson2</groupId>
>     <artifactId>fastjson2</artifactId>
>     <version>2.0.47</version>
> </dependency>
> ```
>
> 如果你更熟悉使用 `jackson`（项目已有），可以用 `com.fasterxml.jackson.databind.ObjectMapper` 替代 `JSON.toJSONString()`，效果相同。

---

### 3.4 Mapper 接口 — SysLogMapper.java

**路径：** `mall-sys/src/main/java/com/mall/mallsys/modules/system/mapper/SysLogMapper.java`

日志查询功能较为简单，直接使用 MyBatis-Plus 内置的 `BaseMapper` 即可，不需要自定义 SQL。

```java
package com.mall.mallsys.modules.system.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.mall.mallsys.modules.system.entity.SysLog;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface SysLogMapper extends BaseMapper<SysLog> {
}
```

**要点说明：** 只需要继承 `BaseMapper<SysLog>`，MyBatis-Plus 会自动提供 CRUD 方法，无需手写。

---

### 3.5 Service 接口 — SysLogService.java

**路径：** `mall-sys/src/main/java/com/mall/mallsys/modules/system/service/SysLogService.java`

```java
package com.mall.mallsys.modules.system.service;

import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.baomidou.mybatisplus.extension.service.IService;
import com.mall.mallsys.modules.system.entity.SysLog;

import java.time.LocalDateTime;

public interface SysLogService extends IService<SysLog> {

    /**
     * 分页查询操作日志（支持条件筛选）
     *
     * @param page      分页对象
     * @param username  操作人（模糊查询，可为 null）
     * @param operation 操作描述（模糊查询，可为 null）
     * @param status    操作状态：0-失败，1-成功，null-不限
     * @param startTime 开始时间（可为 null）
     * @param endTime   结束时间（可为 null）
     */
    Page<SysLog> getLogPage(Page<SysLog> page, String username, String operation,
                            Integer status, LocalDateTime startTime, LocalDateTime endTime);
}
```

**要点说明：**
- 继承 `IService<SysLog>` — 获得 MyBatis-Plus 内置的 CRUD 方法（`save`, `removeById` 等）。
- 额外定义了 `getLogPage` 方法 — 支持多条件分页查询。

---

### 3.6 Service 实现 — SysLogServiceImpl.java

**路径：** `mall-sys/src/main/java/com/mall/mallsys/modules/system/service/impl/SysLogServiceImpl.java`

```java
package com.mall.mallsys.modules.system.service.impl;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.mall.mallsys.modules.system.entity.SysLog;
import com.mall.mallsys.modules.system.mapper.SysLogMapper;
import com.mall.mallsys.modules.system.service.SysLogService;
import org.springframework.stereotype.Service;
import org.springframework.util.StringUtils;

import java.time.LocalDateTime;

@Service
public class SysLogServiceImpl extends ServiceImpl<SysLogMapper, SysLog> implements SysLogService {

    @Override
    public Page<SysLog> getLogPage(Page<SysLog> page, String username, String operation,
                                   Integer status, LocalDateTime startTime, LocalDateTime endTime) {
        LambdaQueryWrapper<SysLog> wrapper = new LambdaQueryWrapper<>();

        // 按操作人模糊查询
        wrapper.like(StringUtils.hasText(username), SysLog::getUsername, username);

        // 按操作描述模糊查询
        wrapper.like(StringUtils.hasText(operation), SysLog::getOperation, operation);

        // 按状态精确查询
        if (status != null) {
            wrapper.eq(SysLog::getStatus, status);
        }

        // 按时间范围查询
        if (startTime != null) {
            wrapper.ge(SysLog::getCreateTime, startTime);
        }
        if (endTime != null) {
            wrapper.le(SysLog::getCreateTime, endTime);
        }

        // 按创建时间倒序排列（最新的在前面）
        wrapper.orderByDesc(SysLog::getCreateTime);

        return baseMapper.selectPage(page, wrapper);
    }
}
```

**要点说明：**

| 方法 | 含义 |
|------|------|
| `wrapper.like(condition, column, value)` | 条件模糊查询（condition 为 true 时才生效） |
| `wrapper.eq(column, value)` | 精确匹配 |
| `wrapper.ge(column, value)` | 大于等于（>=） |
| `wrapper.le(column, value)` | 小于等于（<=） |
| `wrapper.orderByDesc(column)` | 倒序排列 |

---

### 3.7 Controller — SysLogController.java

**路径：** `mall-sys/src/main/java/com/mall/mallsys/modules/system/controller/SysLogController.java`

```java
package com.mall.mallsys.modules.system.controller;

import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.mall.mallsys.common.result.Result;
import com.mall.mallsys.modules.system.entity.SysLog;
import com.mall.mallsys.modules.system.service.SysLogService;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.extern.slf4j.Slf4j;
import org.springframework.format.annotation.DateTimeFormat;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.web.bind.annotation.*;

import java.time.LocalDateTime;
import java.util.List;

@Slf4j
@RestController
@RequestMapping("/api/system/log")
@Tag(name = "操作日志管理")
@PreAuthorize("hasAuthority('sys:log:list')")
public class SysLogController {

    private final SysLogService sysLogService;

    public SysLogController(SysLogService sysLogService) {
        this.sysLogService = sysLogService;
    }

    /**
     * 分页查询操作日志
     */
    @GetMapping("/page")
    @Operation(summary = "分页查询操作日志")
    public Result<Page<SysLog>> page(
            @RequestParam(defaultValue = "1") Integer current,
            @RequestParam(defaultValue = "10") Integer size,
            @RequestParam(required = false) String username,
            @RequestParam(required = false) String operation,
            @RequestParam(required = false) Integer status,
            @RequestParam(required = false) @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss") LocalDateTime startTime,
            @RequestParam(required = false) @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss") LocalDateTime endTime
    ) {
        Page<SysLog> page = sysLogService.getLogPage(
                new Page<>(current, size), username, operation, status, startTime, endTime
        );
        return Result.success(page);
    }

    /**
     * 查询日志详情
     */
    @GetMapping("/{id}")
    @Operation(summary = "查询日志详情")
    public Result<SysLog> getById(@PathVariable Long id) {
        return Result.success(sysLogService.getById(id));
    }

    /**
     * 批量删除日志
     */
    @DeleteMapping
    @Operation(summary = "批量删除日志")
    public Result<Void> deleteBatch(@RequestBody List<Long> ids) {
        sysLogService.removeByIds(ids);
        return Result.success();
    }

    /**
     * 清空所有日志（谨慎使用）
     */
    @DeleteMapping("/clear")
    @Operation(summary = "清空所有日志")
    public Result<Void> clearAll() {
        sysLogService.remove(new com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper<>());
        return Result.success();
    }
}
```

**要点说明：**
- `@PreAuthorize("hasAuthority('sys:log:list')")` — 类级别权限控制，只有拥有 `sys:log:list` 权限的用户才能访问。
- `@DateTimeFormat` — 自动将前端传来的时间字符串转为 `LocalDateTime` 对象。
- 日志是只读数据，所以**不提供** `@PostMapping`（新增）和 `@PutMapping`（修改）接口。

---

### 3.8 DTO（可选） — LogQueryDTO.java

如果你希望接口参数更整洁，可以用 DTO 对象替代多个 `@RequestParam`。这里提供两种写法，任选其一：

**路径：** `mall-sys/src/main/java/com/mall/mallsys/modules/system/dto/LogQueryDTO.java`

```java
package com.mall.mallsys.modules.system.dto;

import lombok.Data;
import org.springframework.format.annotation.DateTimeFormat;

import java.time.LocalDateTime;

/**
 * 日志查询参数 DTO
 */
@Data
public class LogQueryDTO {
    private Integer current = 1;
    private Integer size = 10;
    private String username;
    private String operation;
    private Integer status;

    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime startTime;

    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime endTime;
}
```

**使用 DTO 的 Controller 写法（替换上面的 page 方法）：**

```java
@GetMapping("/page")
@Operation(summary = "分页查询操作日志")
public Result<Page<SysLog>> page(LogQueryDTO query) {
    Page<SysLog> page = sysLogService.getLogPage(
            new Page<>(query.getCurrent(), query.getSize()),
            query.getUsername(), query.getOperation(),
            query.getStatus(), query.getStartTime(), query.getEndTime()
    );
    return Result.success(page);
}
```

> **建议：如果你刚开始学，可以先用 `@RequestParam` 的版本，更直观。**

---

### 3.9 在现有 Controller 方法上添加 @Log 注解

AOP 日志切面已经就绪，现在你可以在任何 Controller 方法上添加 `@Log` 注解来自动记录日志。

**示例 — 给 SysUserController 的方法添加注解：**

```java
// mall-sys/src/main/java/com/mall/mallsys/modules/system/controller/SysUserController.java

@Log("新增用户")
@PostMapping
@Operation(summary = "新增用户")
public Result<Void> add(@Validated @RequestBody SysUser user) {
    user.setPassword(passwordEncoder.encode(user.getPassword()));
    sysUserService.save(user);
    return Result.success();
}

@Log("修改用户")
@PutMapping("/{id}")
@Operation(summary = "修改用户")
public Result<Void> update(@PathVariable Long id, @Validated @RequestBody SysUser user) {
    user.setId(id);
    sysUserService.updateById(user);
    return Result.success();
}

@Log("删除用户")
@DeleteMapping("/{id}")
@Operation(summary = "删除用户")
public Result<Void> delete(@PathVariable Long id) {
    sysUserService.removeById(id);
    return Result.success();
}
```

同理，`SysRoleController`、`SysMenuController` 等其他 Controller 的方法也可以添加 `@Log` 注解。

---

## 4 前端开发

### 4.1 API 接口 — log.ts

**路径：** `mall-sys-web/src/api/log.ts`

```typescript
import request from '@/utils/request'

export interface SysLog {
  id?: number
  username?: string
  operation?: string
  method?: string
  params?: string
  time?: number
  ip?: string
  browser?: string
  status?: number
  errorMsg?: string
  createTime?: string
}

export interface LogQueryParams {
  current?: number
  size?: number
  username?: string
  operation?: string
  status?: number
  startTime?: string
  endTime?: string
}

export interface PageResult<T> {
  records: T[]
  total: number
  size: number
  current: number
  pages: number
}

/** 分页查询操作日志 */
export function getLogPage(params: LogQueryParams) {
  return request.get<PageResult<SysLog>>('/system/log/page', { params })
}

/** 查询日志详情 */
export function getLogById(id: number) {
  return request.get<SysLog>(`/system/log/${id}`)
}

/** 批量删除日志 */
export function deleteLogBatch(ids: number[]) {
  return request.delete<void>('/system/log', { data: ids })
}

/** 清空所有日志 */
export function clearAllLogs() {
  return request.delete<void>('/system/log/clear')
}
```

**要点说明：**
- `request` 已经封装了 baseURL（`/api`），所以路径写 `/system/log/page` 即可。
- `delete` 请求传递 body 参数时，使用 `{ data: ids }`。
- `PageResult` 接口在其他 API 文件中也定义过，如果想复用可以从 `user.ts` 中导入。

---

### 4.2 日志列表页面 — LogList.vue

**路径：** `mall-sys-web/src/views/system/log/LogList.vue`

```vue
<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import dayjs from 'dayjs'
import {
  getLogPage,
  deleteLogBatch,
  clearAllLogs,
  type SysLog,
} from '@/api/log'

const loading = ref(false)
const tableData = ref<SysLog[]>([])
const total = ref(0)
const currentPage = ref(1)
const pageSize = ref(10)

// 查询条件
const queryForm = reactive({
  username: '',
  operation: '',
  status: undefined as number | undefined,
  timeRange: [] as string[],
})

// 已选中的日志 ID（批量删除用）
const selectedIds = ref<number[]>([])

/** 加载数据 */
async function loadData() {
  loading.value = true
  try {
    const res = await getLogPage({
      current: currentPage.value,
      size: pageSize.value,
      username: queryForm.username || undefined,
      operation: queryForm.operation || undefined,
      status: queryForm.status,
      startTime: queryForm.timeRange?.[0]
        ? dayjs(queryForm.timeRange[0]).format('YYYY-MM-DD HH:mm:ss')
        : undefined,
      endTime: queryForm.timeRange?.[1]
        ? dayjs(queryForm.timeRange[1]).endOf('day').format('YYYY-MM-DD HH:mm:ss')
        : undefined,
    })
    tableData.value = res.data.records || []
    total.value = res.data.total || 0
  } catch {
    tableData.value = []
    total.value = 0
  } finally {
    loading.value = false
  }
}

onMounted(loadData)

/** 搜索 */
function handleSearch() {
  currentPage.value = 1
  loadData()
}

/** 重置 */
function handleReset() {
  queryForm.username = ''
  queryForm.operation = ''
  queryForm.status = undefined
  queryForm.timeRange = []
  currentPage.value = 1
  loadData()
}

/** 查看日志详情 */
const detailVisible = ref(false)
const detailLog = ref<SysLog>({})

function handleViewDetail(row: SysLog) {
  detailLog.value = { ...row }
  detailVisible.value = true
}

/** 表格勾选变化 */
function handleSelectionChange(selection: SysLog[]) {
  selectedIds.value = selection.map((item) => item.id!).filter((id) => id != null)
}

/** 批量删除 */
function handleBatchDelete() {
  if (selectedIds.value.length === 0) {
    ElMessage.warning('请先选择要删除的日志')
    return
  }
  ElMessageBox.confirm(
    `确认删除选中的 ${selectedIds.value.length} 条日志？`,
    '提示',
    { confirmButtonText: '确定', cancelButtonText: '取消', type: 'warning' }
  ).then(async () => {
    await deleteLogBatch(selectedIds.value)
    ElMessage.success('删除成功')
    loadData()
  }).catch(() => {})
}

/** 清空全部日志 */
function handleClearAll() {
  ElMessageBox.confirm('确认清空所有操作日志？此操作不可恢复！', '危险操作', {
    confirmButtonText: '确认清空',
    cancelButtonText: '取消',
    type: 'error',
  }).then(async () => {
    await clearAllLogs()
    ElMessage.success('已清空所有日志')
    loadData()
  }).catch(() => {})
}

/** 分页切换 */
function handlePageChange(page: number) {
  currentPage.value = page
  loadData()
}

function handleSizeChange(size: number) {
  pageSize.value = size
  currentPage.value = 1
  loadData()
}

/** 执行耗时显示（格式化） */
function formatTime(ms?: number): string {
  if (ms == null) return '-'
  if (ms < 1000) return `${ms}ms`
  return `${(ms / 1000).toFixed(2)}s`
}
</script>

<template>
  <div class="page-container">
    <el-card>
      <!-- 搜索栏 -->
      <el-form :inline="true">
        <el-form-item label="操作人">
          <el-input
            v-model="queryForm.username"
            placeholder="请输入操作人"
            clearable
            style="width: 160px"
          />
        </el-form-item>
        <el-form-item label="操作描述">
          <el-input
            v-model="queryForm.operation"
            placeholder="请输入操作描述"
            clearable
            style="width: 160px"
          />
        </el-form-item>
        <el-form-item label="状态">
          <el-select v-model="queryForm.status" placeholder="全部" clearable style="width: 120px">
            <el-option :value="1" label="成功" />
            <el-option :value="0" label="失败" />
          </el-select>
        </el-form-item>
        <el-form-item label="操作时间">
          <el-date-picker
            v-model="queryForm.timeRange"
            type="daterange"
            range-separator="至"
            start-placeholder="开始日期"
            end-placeholder="结束日期"
            value-format="YYYY-MM-DD"
            style="width: 240px"
          />
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="handleSearch">搜索</el-button>
          <el-button @click="handleReset">重置</el-button>
        </el-form-item>
      </el-form>

      <!-- 操作按钮 -->
      <div style="margin-bottom: 12px">
        <el-button type="danger" plain @click="handleBatchDelete"
          :disabled="selectedIds.length === 0">
          批量删除
        </el-button>
        <el-button type="danger" @click="handleClearAll">清空全部</el-button>
      </div>

      <!-- 数据表格 -->
      <el-table
        v-loading="loading"
        :data="tableData"
        stripe
        border
        @selection-change="handleSelectionChange"
      >
        <el-table-column type="selection" width="50" />
        <el-table-column prop="id" label="ID" width="70" />
        <el-table-column prop="username" label="操作人" width="100" />
        <el-table-column prop="operation" label="操作描述" width="120" />
        <el-table-column prop="ip" label="IP 地址" width="140" />
        <el-table-column prop="status" label="状态" width="80">
          <template #default="{ row }">
            <el-tag :type="row.status === 1 ? 'success' : 'danger'">
              {{ row.status === 1 ? '成功' : '失败' }}
            </el-tag>
          </template>
        </el-table-column>
        <el-table-column label="耗时" width="100">
          <template #default="{ row }">
            <span>{{ formatTime(row.time) }}</span>
          </template>
        </el-table-column>
        <el-table-column prop="createTime" label="操作时间" width="180" />
        <el-table-column label="操作" width="100" fixed="right">
          <template #default="{ row }">
            <el-button link type="primary" size="small" @click="handleViewDetail(row)">
              详情
            </el-button>
          </template>
        </el-table-column>
      </el-table>

      <!-- 分页 -->
      <el-pagination
        class="pagination"
        v-model:current-page="currentPage"
        v-model:page-size="pageSize"
        :total="total"
        :page-sizes="[10, 20, 50]"
        layout="total, sizes, prev, pager, next"
        @current-change="handlePageChange"
        @size-change="handleSizeChange"
      />
    </el-card>

    <!-- 日志详情弹窗 -->
    <el-dialog v-model="detailVisible" title="日志详情" width="650px" destroy-on-close>
      <el-descriptions :column="2" border>
        <el-descriptions-item label="操作人">
          {{ detailLog.username || '-' }}
        </el-descriptions-item>
        <el-descriptions-item label="操作描述">
          {{ detailLog.operation || '-' }}
        </el-descriptions-item>
        <el-descriptions-item label="IP 地址">
          {{ detailLog.ip || '-' }}
        </el-descriptions-item>
        <el-descriptions-item label="状态">
          <el-tag :type="detailLog.status === 1 ? 'success' : 'danger'">
            {{ detailLog.status === 1 ? '成功' : '失败' }}
          </el-tag>
        </el-descriptions-item>
        <el-descriptions-item label="请求方法" :span="2">
          {{ detailLog.method || '-' }}
        </el-descriptions-item>
        <el-descriptions-item label="执行耗时">
          {{ formatTime(detailLog.time) }}
        </el-descriptions-item>
        <el-descriptions-item label="操作时间">
          {{ detailLog.createTime || '-' }}
        </el-descriptions-item>
        <el-descriptions-item label="浏览器" :span="2">
          {{ detailLog.browser || '-' }}
        </el-descriptions-item>
        <el-descriptions-item label="请求参数" :span="2">
          <pre style="max-height: 200px; overflow: auto; margin: 0;
              background: #f5f5f5; padding: 8px; border-radius: 4px;
              font-size: 12px; white-space: pre-wrap; word-break: break-all;">
{{ detailLog.params || '-' }}
          </pre>
        </el-descriptions-item>
        <el-descriptions-item v-if="detailLog.status === 0 && detailLog.errorMsg"
          label="错误信息" :span="2">
          <pre style="max-height: 150px; overflow: auto; margin: 0;
              background: #fff2f0; padding: 8px; border-radius: 4px;
              font-size: 12px; color: #cf1322; white-space: pre-wrap; word-break: break-all;">
{{ detailLog.errorMsg }}
          </pre>
        </el-descriptions-item>
      </el-descriptions>
      <template #footer>
        <el-button @click="detailVisible = false">关闭</el-button>
      </template>
    </el-dialog>
  </div>
</template>

<style scoped>
.page-container {
  padding: 20px;
}

.pagination {
  margin-top: 16px;
  justify-content: flex-end;
}

pre {
  font-family: 'Courier New', Courier, monospace;
}
</style>
```

**要点说明：**

| 功能 | 实现方式 |
|------|----------|
| 日期范围筛选 | `el-date-picker` + `type="daterange"` |
| 批量选择删除 | `el-table` 的 `type="selection"` + `@selection-change` |
| 状态标签 | `el-tag` 根据 `status` 值切换 `type` |
| 耗时格式化 | `formatTime` 方法，小于 1 秒显示 ms，超过显示 s |
| 详情弹窗 | `el-descriptions` 组件展示详细信息 |
| 参数/错误信息展示 | `<pre>` 标签保留 JSON 原始格式 |

---

### 4.3 注册路由

**修改文件：** `mall-sys-web/src/router/index.ts`

在 `children` 数组中添加日志管理的路由：

```typescript
{
  path: 'system/log',
  name: 'SystemLog',
  component: () => import('@/views/system/log/LogList.vue'),
  meta: { title: '日志管理' },
},
```

**完整效果（插入到 Home 路由之后、system/user 之前）：**

```typescript
children: [
  {
    path: 'home',
    name: 'Home',
    component: () => import('@/views/home/Home.vue'),
    meta: { title: '首页' },
  },
  {
    path: 'system/log',
    name: 'SystemLog',
    component: () => import('@/views/system/log/LogList.vue'),
    meta: { title: '日志管理' },
  },
  {
    path: 'system/user',
    name: 'SystemUser',
    component: () => import('@/views/system/user/UserList.vue'),
    meta: { title: '用户管理' },
  },
  // ... 其他路由
]
```

---

## 5 菜单注册

### 5.1 插入菜单数据

在数据库中执行以下 SQL，为日志管理模块注册菜单和权限：

```sql
USE `mall_sys`;

-- 1. 在"系统管理"目录下新增"日志管理"菜单
INSERT INTO `sys_menu` (`parent_id`, `name`, `path`, `component`, `permission`, `type`, `icon`, `sort`, `visible`)
VALUES (1, '日志管理', 'log', 'system/log/index', 'sys:log:list', 1, 'Document', 4, 1);

-- 2. 获取刚插入的菜单 ID（日志管理菜单）
SET @log_menu_id = LAST_INSERT_ID();

-- 3. 新增按钮权限
INSERT INTO `sys_menu` (`parent_id`, `name`, `path`, `component`, `permission`, `type`, `icon`, `sort`, `visible`)
VALUES
    (@log_menu_id, '日志查询', '', NULL, 'sys:log:query', 2, NULL, 1, 1),
    (@log_menu_id, '日志删除', '', NULL, 'sys:log:delete', 2, NULL, 2, 1);

-- 4. 让超级管理员自动拥有新增的日志菜单权限
INSERT INTO `sys_role_menu` (`role_id`, `menu_id`)
SELECT 1, id FROM `sys_menu` WHERE `parent_id` = @log_menu_id OR id = @log_menu_id;
```

### 5.2 菜单数据说明

| 菜单 | 类型 | permission |
|------|------|------------|
| 日志管理 | 菜单 (1) | `sys:log:list` |
| 日志查询 | 按钮 (2) | `sys:log:query` |
| 日志删除 | 按钮 (2) | `sys:log:delete` |

---

## 6 测试验证

### 6.1 后端测试（通过 Swagger 或 Postman）

启动后端后访问 `http://localhost:8086/swagger-ui/index.html`。

**步骤：**

1. **先登录获取 Token**
   - 调用 `POST /api/system/login`，Body 传入 `{"username":"admin","password":"123456"}`
   - 复制返回的 token 值
   - 点击右上角 "Authorize" 按钮，输入 `Bearer <token>` 进行授权

2. **测试日志分页查询**
   - 调用 `GET /api/system/log/page?current=1&size=10`
   - 应该能查到日志列表（前提是你已经操作过带 `@Log` 注解的接口）

3. **触发日志记录**
   - 调用任意加了 `@Log` 注解的接口（如 "新增用户"）
   - 再次调用日志分页查询，应该能看到新增的日志记录

### 6.2 前端测试

1. 启动前端：`npm run dev`
2. 访问 `http://localhost:5173`，使用 `admin / 123456` 登录
3. 左侧菜单中应该能看到 **"系统管理 > 日志管理"**
4. 点击"日志管理"进入页面，验证以下功能：
   - [ ] 日志列表正常显示
   - [ ] 按操作人筛选
   - [ ] 按操作描述筛选
   - [ ] 按状态筛选（成功/失败）
   - [ ] 按时间范围筛选
   - [ ] 点击"详情"查看完整日志信息
   - [ ] 批量删除日志
   - [ ] 清空全部日志
   - [ ] 分页功能正常

### 6.3 常见排错

| 问题 | 原因 | 解决方法 |
|------|------|----------|
| 操作后没有生成日志 | 方法上没有 `@Log` 注解 | 在 Controller 方法上添加 `@Log("操作描述")` |
| 操作后没有生成日志 | `pom.xml` 缺少 `spring-boot-starter-aop` | 添加 AOP 依赖 |
| AOP 报错 `No bean named 'sysLogService'` | Service 未加 `@Service` 注解 | 确认 `SysLogServiceImpl` 上有 `@Service` |
| 日志的 username 为 null | 方法被调用时 SecurityContext 中无用户信息 | 确认请求带上了正确的 JWT Token |
| 前端页面 404 | 路由未注册或路径不匹配 | 检查 `router/index.ts` 中是否添加了日志路由 |
| 前端页面空白 | `dayjs` 未安装 | 运行 `npm install dayjs` |
| 菜单不显示 | 菜单未注册到数据库，或未关联到角色 | 执行菜单 SQL，并确认 `sys_role_menu` 中有对应记录 |
| `@PreAuthorize` 导致 403 | 当前用户没有 `sys:log:list` 权限 | 在 `sys_role_menu` 中将日志菜单关联到超级管理员 |

---

## 附录：完整文件路径速查

### 需要新建的文件

```
mall-sys/
└── src/main/java/com/mall/mallsys/
    ├── common/
    │   ├── annotation/
    │   │   └── Log.java                          [新建]
    │   └── aspect/
    │       └── LogAspect.java                    [新建]
    └── modules/system/
        ├── entity/
        │   └── SysLog.java                       [新建]
        ├── mapper/
        │   └── SysLogMapper.java                 [新建]
        ├── service/
        │   ├── SysLogService.java                [新建]
        │   └── impl/
        │       └── SysLogServiceImpl.java        [新建]
        ├── controller/
        │   └── SysLogController.java             [新建]
        └── dto/
            └── LogQueryDTO.java                  [可选，新建]

mall-sys-web/
└── src/
    ├── api/
    │   └── log.ts                                [新建]
    ├── views/system/log/
    │   └── LogList.vue                           [新建]
    └── router/
        └── index.ts                              [修改]
```

### 需要修改的文件

- `mall-sys/pom.xml` — 添加 `fastjson2` 依赖（如项目中还没有）
- `mall-sys/src/.../controller/SysUserController.java` — 在方法上添加 `@Log` 注解
- `mall-sys/src/.../controller/SysRoleController.java` — 同上
- `mall-sys/src/.../controller/SysMenuController.java` — 同上

### 需要执行的 SQL

- 建表 SQL（`sys_log` 表）
- 菜单 SQL（日志管理菜单 + 按钮权限）

---

**开发顺序建议：**

```
第 1 步：执行 SQL 建表（sys_log）
第 2 步：创建后端 Entity → Mapper → Service → Controller
第 3 步：创建自定义注解 @Log
第 4 步：创建 AOP 切面 LogAspect
第 5 步：在已有 Controller 方法上添加 @Log 注解
第 6 步：后端启动测试（Swagger 验证日志是否生成）
第 7 步：创建前端 API（log.ts）
第 8 步：创建前端页面（LogList.vue）+ 注册路由
第 9 步：执行菜单 SQL
第 10 步：前端启动测试（完整功能验证）
```

祝开发顺利！按照上述步骤一步步操作即可完成日志管理模块。